page.insert_html :top, :changetable, "<td>...more errors...</td>"
page.insert_html :top, :changetable, :partial => 'recent_errors', :object => @errors

page[:changetable].visual_effect :highlight

# Hide 'show errors' link
page.select("#sick a").each do |a|
  a.hide
end

